

/* 
const sendMail = (content: string) => {
    console.log(``)
}

function createNode(){
    console.log("Node.")
}

let linkedinlist;
const res = get5000users();
res.forEach((email: string) => {
    linkedinlist.next = createNode()
    sendMail(email);
})

let currentNode = linkedlist;
while(currentNode.next !== null) {
    currentNode = currentNode.next;
}

 */